package com.spoony.spoony_server.adapter.dto.user;

public record UserSearchResultDTO(
                                  String userName,
                                  String regionName
                                  ) {
}
